import { StyledTopbar } from './styles';
import Logo from '../svg/logo';
const Topbar = (params) => {
    return (
        <StyledTopbar>
            <Logo />
        </StyledTopbar>
    )
}
export default Topbar;